services:
  # CONFIGURACIÓN DEL BACKEND (Spring Boot)
  - type: web
    name: backend-vkr
    runtime: docker # Usamos Docker para asegurar que use Java 17
    rootDir: backend
    plan: free # Aseguramos que use el plan gratuito
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod